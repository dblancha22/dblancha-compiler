testfunc: function void (x: integer, y: integer, z: integer) = {
    r: integer;

    b: boolean;

    r = (x + y) / z;
    r = x + (y + z);
    b = ( false  ||  true ) && ( true  ||  false );
    r = x + y / z;
    r = (x + y) + z;
    b = x + y < x + z;
    b =  false  &&  true  ||  true  &&  false ;

}